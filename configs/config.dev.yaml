# Development environment overrides
db:
  host: "localhost"
  port: "5432"
  database: "claims_db_dev"
  username: "postgres"
  password: "postgres"
  maxconns: 5
  minconns: 1

trace:
  enabled: false  # Disable tracing in development

cache:
  redisserver: "localhost:6379"
  redispassword: ""
  isredisenabled: false  # Disable Redis in development
  islocalcacheenabled: true  # Use local cache only

# Override info for development
info:
  name: "claims-api-dev"
  version: "1.0.0-dev"
